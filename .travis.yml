language: node_js
node_js: stable
addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
before_script:
  - npm install -g bower web-component-tester
  - bower install
script:
  - xvfb-run wct
dist: trusty
sudo: required
